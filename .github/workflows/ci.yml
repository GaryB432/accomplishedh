name: CI

on:
  push:
  pull_request:

env:
  VERCEL_GIT_COMMIT_REF: TBD
  VERCEL_GIT_COMMIT_SHA: TBD
  VERCEL_GIT_COMMIT_MESSAGE: TBD

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: true
      - name: Playwright Browsers
        working-directory: ./apps/web
        run: pnpm exec playwright install
      - name: Check
        run: pnpm --filter=@accomplishedh/web --if-present run check
      - name: Lint
        run: pnpm --filter=@accomplishedh/web --if-present run lint
      - name: Build
        run: pnpm --filter=@accomplishedh/web --if-present run build
      - name: Test
        run: pnpm --filter=@accomplishedh/web --if-present --use-stderr run test
